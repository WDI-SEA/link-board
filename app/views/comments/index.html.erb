<h1><%= link_to @post.title, @post.link %></h1>

<%= link_to 'New Comment', new_post_comment_path(@post), class: 'btn btn-primary' %>

<% @post.comment.each do |comment| %>
<div class="well">
  <p><%= comment.content %></p>
  <strong>By <%= comment.user.name %></strong>
 <!--  <%= render partial: 'comments/voting', locals: {post: post} %> -->
 <!-- this line above was added to render voting code from partial folder added to comments directory. Pretty sure I didn't need to create another, but I tried anyway to get page to work.
</div> -->
<% end %>