<div class="container-fluid">
  <h1> Sign up </h1>

  <%= bootstrap_form_for @user do |u| %>
      <%= c.text_field :email %>
      <%= c.text_field :name %>
      <%= c.text_field :password %>
    <%= c.submit %>
  <% end %>

</div>