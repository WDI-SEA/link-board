<h1> NEW POST </h1>

<%= form_for :posts do |p| %>
  <%= p.label :title %>
  <%= p.text_field :title %>

  <%= p.label :url %>
  <%= p.text_field :url %>

  <%= p.label :user_id %>
  <%= p.text_field :user_id %>



  <%= p.submit %>

<% end %>