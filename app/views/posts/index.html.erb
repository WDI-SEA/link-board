

<ol>
  <%  @posts.each do |p| %>
    <li>
    <a href="<%= p.link  %>"> <%= p.title  %></a>
    <br>
    <span class="smaller"> <%= rand(10..1000) %> points by <%= @current_user.name  %> <%= rand(2..24) %> hours ago </span>
    </li>
  <% end %>
</ol>