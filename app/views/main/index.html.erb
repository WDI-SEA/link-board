<h1>Hacker News</h1>
<p>Find me in app/views/main/index.html.erb</p>

<% @posts.each do |post| %>  

<h2 class="well"><%= post.title %></h2>

  
<% end %>

<%= link_to('Signup', signup_path, class: 'btn btn-primary') %>
<%= link_to('Login', login_path, class: 'btn btn-primary') %>
<%= link_to('Profile', profile_path, class: 'btn btn-success') %>
<%= link_to('Add New Post', posts_new_path, class: 'btn btn-primary') %>
<%= link_to('Logout', logout_path, method: :delete, class: 'btn btn-danger') %>
